# Backend API URL (proxied through Vite, no /api prefix as it's added by backend routes)
VITE_API_URL=

# Socket.IO URL (proxied through Vite to avoid mixed content)
VITE_SOCKET_URL=

# Note: Both API and Socket.IO go through Vite proxy (HTTPS → HTTP)

# App Title
VITE_APP_TITLE=ExamTrack Dev
